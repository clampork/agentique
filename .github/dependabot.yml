version: 2
updates:
  # The workflow pins actions by major version, which picks up patches automatically but
  # not majors. This opens a PR when a new major lands.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
